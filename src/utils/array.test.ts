// import { shuffle } from './array';

describe('array', () => {
  it('shuffle 함수를 실행하면 배열 원소의 순서가 무작위로 섞인다.', () => {
    // const arr = [1, 2, 3, 4, 5, 6, 7];
    // const shuffledArr = shuffle(arr);
    // expect(arr).not.toEqual(shuffledArr);
  });
});
